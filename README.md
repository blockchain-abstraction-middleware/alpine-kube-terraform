# Alpine image with `kubectl` and `terraform` 0.12.x

## Overview
Build image to speed up deployments 

- Build
```shell
docker build -t hello/alpine-kube-terraform
```

- Run
```shell
docker run -it hello/alpine-kube-terraform
```
